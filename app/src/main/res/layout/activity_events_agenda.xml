<?xml version="1.0" encoding="utf-8"?>
<!-- Events agenda: upcoming + overdue items grouped by day. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/shs_night"
    android:orientation="vertical">

    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/agenda_toolbar"
        style="@style/SHS.TopBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:navigationIcon="@drawable/ic_back"
        app:title="@string/event_agenda_title" />

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/agenda_list"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clipToPadding="false"
            android:padding="@dimen/shs_screen_padding"
            android:paddingBottom="@dimen/shs_bottom_nav_height"
            android:scrollbars="vertical" />

        <TextView
            android:id="@+id/agenda_empty"
            style="@style/SHS.Text.Subtitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:gravity="center"
            android:text="@string/agenda_empty"
            android:textColor="@color/shs_text_muted"
            android:visibility="gone" />

        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/agenda_add"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|end"
            android:layout_margin="@dimen/shs_space_lg"
            android:layout_marginBottom="@dimen/shs_bottom_nav_height"
            android:contentDescription="@string/event_new"
            android:src="@android:drawable/ic_input_add"
            app:backgroundTint="@color/shs_cyan"
            app:tint="@color/shs_night" />
    </FrameLayout>
</LinearLayout>
